interface:
  display_name: "Context Canvas"
  short_description: "Navigate session work and offload retrievable context"
  default_prompt: "Use $context-canvas-checkpoint when an optional task map, explicit reference, or one-shot historical capture would improve navigation or long-context offload. Missing Canvas state or hook identity does not block the underlying task. For activation diagnosis, manual approval of newly installed hooks may be required; inspect /hooks."
