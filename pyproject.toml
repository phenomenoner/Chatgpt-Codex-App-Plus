[project]
name = "chatgpt-codex-app-plus-tools"
version = "0.1.0"
description = "Public-safety tooling for the ChatGPT Codex App Plus bundle"
requires-python = ">=3.11"
license = "MIT"

[tool.ruff]
line-length = 100
target-version = "py311"
